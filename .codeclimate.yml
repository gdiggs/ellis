engines:
  rubocop:
    enabled: true
  eslint:
    enabled: false
  csslint:
    enabled: false
  bundler-audit:
    enabled: true
ratings:
  paths:
    - "app/**"
    - Gemfile.lock
exclude_paths:
  - "vendor/**"
