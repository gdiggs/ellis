engines:
  rubocop:
    enabled: true
  eslint:
    enabled: true
  bundler-audit:
    enabled: true
  fixme:
    enabled: true
  brakeman:
    enabled: true
ratings:
  paths:
    - "app/**/*"
    - Gemfile.lock
exclude_paths:
  - "app/foo.rb"
  - "**.ru"
  - "vendor/**"
